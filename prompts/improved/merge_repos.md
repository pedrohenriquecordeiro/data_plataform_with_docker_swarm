Modify `data_plataform/project`:

- Implement custom Airflow image building in `Makefile`.
- Add local Airflow entries to `.env.example`.
- Automate Step 2 of `data_plataform/project/infra/docs/deployment_guide.md` using `Makefile`.
- Update `data_plataform/project/infra/docs/deployment_guide.md` to document:
    1. Comprehensive deployment.
    2. Direct component deployment.
- Update all project references.
